# Poetry

--start
poetry init  <set pyproject.toml>
poetry shell <run PS as admin from in the folder>
poetry lock
poetry update
а чё так сложно????

--add
poetry add *

# Overall
poetry shell -> "flask --app..."/"python..." run from dir (cd $PSScriptRoot) as admin (shortcut)



# Docker
docker build -t hw1_dockered .
docker run -p 5000:5000 hw1_dockered (call localhost:5000 works)

/tests/ - системное тестирование на минималках
